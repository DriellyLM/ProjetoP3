<h1>Listing produtos</h1>

<table>
  <thead>
    <tr>
      <th>Quantidade</th>
      <th>Numeroee</th>
      <th>Dataentradaestoque</th>
      <th>Datasaidaestoque</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @produtos.each do |produto| %>
      <tr>
        <td><%= produto.quantidade %></td>
        <td><%= produto.numeroee %></td>
        <td><%= produto.dataentradaestoque %></td>
        <td><%= produto.datasaidaestoque %></td>
        <td><%= link_to 'Show', produto %></td>
        <td><%= link_to 'Edit', edit_produto_path(produto) %></td>
        <td><%= link_to 'Destroy', produto, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Produto', new_produto_path %>
